services:
  - type: web
    name: dateq-api
    runtime: python
    rootDir: src/Backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ORIGIN
        sync: false
